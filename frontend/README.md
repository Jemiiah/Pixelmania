# Pixelmania frontend

Vite + React app. Run from this folder:

```bash
cd frontend
npm install
npm run dev
```

- **Dev:** `npm run dev`
- **Build:** `npm run build`
- **Preview build:** `npm run preview`

Contract ABIs (when needed) live in `../contracts/out/`.
