# Deployer private key (no 0x prefix). Use a test wallet on Alfajores.
PRIVATE_KEY=your_private_key_here

# Pixelmania deploy (required for script/Deploy.s.sol)
TREASURY=0x...
DEV_WALLET=0x...
CFA_ADDRESS=0x...   # Superfluid CFA contract (not forwarder); get via Host.getContract("CFAv1") on Celo

# Celo RPC (for forge script --rpc-url)
ALFAJORES_RPC_URL=https://alfajores-forno.celo-testnet.org
CELO_RPC_URL=https://forno.celo.org
